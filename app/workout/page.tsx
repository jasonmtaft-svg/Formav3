/**
 * Active superset screen — placeholder.
 * Full implementation will pull the active WorkoutSession from context
 * and render SupersetView for the current superset index.
 */
export default function WorkoutPage() {
  return (
    <main className="flex min-h-dvh flex-col items-center justify-center px-6">
      <p className="text-text-secondary">Workout screen — coming soon.</p>
    </main>
  );
}
